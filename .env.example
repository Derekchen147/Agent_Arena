# Server
HOST=0.0.0.0
PORT=8000
DEBUG=true

# Database
DATABASE_URL=sqlite+aiosqlite:///data/agent_arena.db

# Workspaces
WORKSPACES_DIR=workspaces

# 注意：不需要 API Key。
# Agent 通过 CLI 工具调用（如 Claude Code CLI），
# CLI 自身管理认证（如 claude auth login）。
